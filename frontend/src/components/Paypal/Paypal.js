import React, { useEffect, useRef } from 'react';

const Paypal = () => {

    const paypal = useRef()

    useEffect(()=> {
        
    }, [])
    return (
        <div ref={paypal}>
            
        </div>
    );
};

export default Paypal;